#A simple jQuery scrollable element gradient hint, for mobile devices

####To start:
$(my_selector).scroll_hint();

####To stop:
$(my_selector).scroll_hint("destroy");

Currently only supports vertical scrolling.
